name: Python Unit Tests Trigger

on:
  pull_request:
    branches: ["main", "feature*"]
    paths:
      - "python/**"

jobs:
  python-unit-tests-trigger:
    uses: ./python/.github/actions/python-unit-tests.yml
    secrets: inherit
