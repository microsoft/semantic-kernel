release_tracks: [ALPHA]
help_text:
  brief: |
    Import observability policy.
  description: |
    Import an observability policy.

request:
  collection: networkservices.projects.locations.observabilityPolicies
  api_version: v1alpha1

arguments:
  resource:
    spec: !REF googlecloudsdk.command_lib.network_services.resources:observabilityPolicy
    help_text: |
      Name of the observability policy to import.

async:
  collection: networkservices.projects.locations.operations

import:
  abort_if_equivalent: true
  create_if_not_exists: true
  create_request:
    collection: networkservices.projects.locations.observabilityPolicies
    api_version: v1alpha1
    method: create
