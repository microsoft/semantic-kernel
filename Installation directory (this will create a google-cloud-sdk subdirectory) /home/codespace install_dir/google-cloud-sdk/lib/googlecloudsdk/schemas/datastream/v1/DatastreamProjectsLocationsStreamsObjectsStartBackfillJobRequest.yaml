$schema: "http://json-schema.org/draft-06/schema#"

title: datastream v1 DatastreamProjectsLocationsStreamsObjectsStartBackfillJobRequest
  export schema
description: A gcloud export/import command YAML validation schema.
type: object
required:
- object
additionalProperties: false
properties:
  COMMENT:
    type: object
    description: User specified info ignored by gcloud import.
    additionalProperties: false
    properties:
      template-id:
        type: string
      region:
        type: string
      description:
        type: string
      date:
        type: string
      version:
        type: string
  UNKNOWN:
    type: array
    description: Unknown API fields that cannot be imported.
    items:
      type: string
  object:
    description: |-
      The name of the stream object resource to start a backfill job for.
    type: string
  startBackfillJobRequest:
    description: |-
      A StartBackfillJobRequest resource to be passed as the request body.
    type: startbackfilljobrequest
