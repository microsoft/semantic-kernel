<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- THIS PROPERTY GROUP MUST COME FIRST -->
    <AssemblyName>Microsoft.SemanticKernel.Connectors.Onnx</AssemblyName>
    <RootNamespace>$(AssemblyName)</RootNamespace>
    <TargetFrameworks>net8.0;netstandard2.0</TargetFrameworks>
    <VersionSuffix>alpha</VersionSuffix>
    <IsAotCompatible Condition="$([MSBuild]::IsTargetFrameworkCompatible('$(TargetFramework)', 'net7.0'))">true</IsAotCompatible>
    <NoWarn>$(NoWarn);CA1050;RCS1110</NoWarn>
  </PropertyGroup>

  <PropertyGroup>
    <Title>Semantic Kernel - ONNX ConnectorsC</Title>
    <Description>Semantic Kernel connectors for the ONNX runtime on CPU. Contains clients for text embedding generation.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Connectors.Onnx.Cpu\Connectors.Onnx.Cpu.csproj" />
  </ItemGroup>

</Project>
