{{#*inline "UserGoal"}}
{{#message role="user"}}Please show me how to write a Handlebars template that achieves the following goal with the available helpers.

## Goal
{{goal}}

{{#if predefinedArguments}}
### Predefined Variables
Here is a list of predefined variables that can be used within the Handlebars template. These variables may or may not be crucial for the execution of the template and achieving the stated goal.
You can access these variables using the `@root` object (e.g., `@root.variableName`):

{{#each predefinedArguments}}
- "{{@key}}" ({{Type}}): {{Value}}
{{/each}}

Important: this is not a comprehensive list of relevant data. Remember to create variables for key values from the goal as well.
{{/if}}
{{/message}}
{{/inline}}