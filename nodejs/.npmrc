# Default npm registry (uncomment if needed)
registry=https://registry.npmjs.org/

# Artifactory registry (update with your artifactory URL)
# registry=https://your-artifactory-url/artifactory/api/npm/npm-local/

# Always use exact versions for dependencies
save-exact=true

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
