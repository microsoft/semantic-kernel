{
  "schema": 1,
  "type": "completion",
  "description": "Extract entities related to a given topic from a text and return them as a list",
  "completion": {
    "max_tokens": 1024,
    "temperature": 0.0,
    "top_p": 0.0,
    "presence_penalty": 0.0,
    "frequency_penalty": 0.0
  },
  "input": {
    "parameters": [
      {
        "name": "input",
        "description": "The text from which the entities are to be extracted",
        "defaultValue": ""
      },
      {
        "name": "topic",
        "description": "The topic of interest; this will determine which entities are extracted",
        "defaultValue": ""
      },
      {
        "name": "example_entities",
        "description": "A list of example entities from the topic. This can help guide the entity extraction",
        "defaultValue": ""
      }
    ]
  }
}
