# Task Description

1. Please extract a list of entities related to {{$topic}} from between the <context> tags.
2. As the first part of your response, generate a bulleted list of each of the items in (1) together with an explanation of what they are.
3. Go over each item in your bulleted list and read the explanation of what it is. Keep items which are factual nuggets relating to {{$topic}}.
4. Go over each item in your bulleted list and check for duplicates. Keep only one example of each. Duplicates may be:
    - Abbreviations
    - Reuse as adjectives
    - Plurals and related changes
5. Please return the list of items from (6) as a JSON list of dictionaries between a <json_block> and a </json_block> tag, after the bulleted list.
6. Each dictionary should correspond to one of the items from (6) and have two keys, "entity_name" and "reason"
7. The value associated with the "entity_name" key should be the name of the item
8. The value associated with the "reason" key should be the explanation associated with the item
9. Make sure that you return valid JSON for (7)
10. Any quotes appearing within a string in (7) should be escaped by a double backslash.


# Task

Extract entities related to {{$topic}} from the following context. Ensure the content between <json_block> and </json_block> is valid JSON.

<context>
{{$input}}
</context>